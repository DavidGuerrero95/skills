---
applyTo: "**/Dockerfile,**/compose.yaml,**/compose.yml,**/docker-compose.yml"
description: Docker and Docker Compose conventions.
---

# docker — canonical pointers

`/memory` is the source of truth. Read and apply:

- `/memory/stacks/docker-compose.md`

Do not duplicate rules here; update the canonical file under `/memory`.
