---
applyTo: "**/*.sql"
description: SQL schema, migration, and indexing conventions.
---

# sql-migrations — canonical pointers

`/memory` is the source of truth. Read and apply:

- `/memory/stacks/postgresql.md`
- `/memory/skills/database-design/SKILL.md`

Do not duplicate rules here; update the canonical file under `/memory`.
